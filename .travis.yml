sudo: required

language: python
python:
  - '3.6'

services:
  - docker

script:
  - docker build -t informatics-lab/pangeo-notebook:$TRAVIS_COMMIT .

deploy:
  provider: script
  script: deploy.sh
  on:
    branch: master
