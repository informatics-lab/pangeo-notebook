sudo: required

language: python
python:
  - '3.6'

services:
  - docker

script:
  - docker build -t informatics-lab/pangeo-notebook:$TRAVIS_COMMIT .

deploy:
  skip_cleanup: true
  provider: script
  script: sh $TRAVIS_BUILD_DIR/deploy.sh
  on:
    branch: master
